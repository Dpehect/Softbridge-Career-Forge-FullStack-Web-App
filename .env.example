# CareerForge — client-side only (no backend AI keys required)
#
# Public site URL for SEO / Open Graph (set after Vercel deploy)
NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app
#
# No OLLAMA_*, OPENAI_*, or API keys — Transformers.js runs in the browser.
